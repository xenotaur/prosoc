#!/bin/bash
# Build prosoc via Python's build module
# Requires:
# - pip install build
echo "Building the prosoc package..."
set -x

python -m build

# Turn off logging and print a blank line
{ set +x; } 2>/dev/null
echo
