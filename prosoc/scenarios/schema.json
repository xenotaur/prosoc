{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Prosoc Social Navigation Scenario",
  "description": "Schema for a human-authored social navigation scenario card, used to describe contextual situations in which robot navigation behavior is evaluated against the Prosocial Navigation Charter.",
  "type": "object",

  "required": ["id", "name", "summary"],

  "properties": {
    "id": {
      "type": "string",
      "description": "Unique identifier for the scenario. Intended to be stable and suitable for referencing across experiments, datasets, and evaluations."
    },

    "name": {
      "type": "string",
      "description": "Human-readable name for the scenario."
    },

    "summary": {
      "type": "string",
      "description": "Brief natural-language description of the scenario and its salient features."
    },

    "context": {
      "type": "object",
      "description": "Environmental, social, and situational context in which the scenario occurs.",
      "properties": {
        "environment": {
          "type": "object",
          "description": "Physical environment description (e.g., indoor/outdoor, hallway, plaza).",
          "properties": {
            "type": { "type": "string" },
            "setting": { "type": "string" },
            "width": { "type": "string" }
          },
          "additionalProperties": true
        },

        "social_setting": {
          "type": "object",
          "description": "Social context such as formality, norms, or crowd characteristics.",
          "properties": {
            "formality": { "type": "string" },
            "crowd_level": { "type": "string" }
          },
          "additionalProperties": true
        }
      },
      "additionalProperties": true
    },

    "agents": {
      "type": "object",
      "description": "Agents participating in the scenario, including the robot and other entities.",
      "properties": {
        "robot": {
          "type": "object",
          "description": "Description of the robot agent and its assumed capabilities.",
          "properties": {
            "role": { "type": "string" },
            "capabilities": {
              "type": "array",
              "items": { "type": "string" }
            }
          },
          "additionalProperties": true
        },

        "humans": {
          "type": "array",
          "description": "Human agents present in the scenario.",
          "items": {
            "type": "object",
            "properties": {
              "role": { "type": "string" },
              "count": { "type": "integer", "minimum": 1 },
              "attributes": {
                "type": "object",
                "description": "Optional human attributes relevant to the scenario (e.g., mobility, attentiveness).",
                "additionalProperties": true
              }
            },
            "additionalProperties": true
          }
        }
      },
      "additionalProperties": true
    },

    "initial_conditions": {
      "type": "object",
      "description": "Initial spatial, temporal, or situational conditions at the start of the scenario.",
      "additionalProperties": true
    },

    "expected_behaviors": {
      "type": "object",
      "description": "Scenario-specific normative expectations describing appropriate and inappropriate robot behavior.",
      "properties": {
        "must": {
          "type": "array",
          "description": "Behaviors that are required for acceptable performance in this scenario.",
          "items": { "type": "string" }
        },
        "should": {
          "type": "array",
          "description": "Behaviors that are preferred or encouraged but not strictly required.",
          "items": { "type": "string" }
        },
        "should_not": {
          "type": "array",
          "description": "Behaviors that are discouraged or indicative of poor performance.",
          "items": { "type": "string" }
        }
      },
      "additionalProperties": false
    },

    "relevant_principles": {
      "type": "array",
      "description": "Identifiers of charter principles (e.g., P1, P3) that are particularly relevant to this scenario.",
      "items": {
        "type": "string",
        "pattern": "^P[0-9]+$"
      }
    },

    "evaluation_notes": {
      "type": "string",
      "description": "Additional guidance for evaluators interpreting behavior within this scenario."
    }
  },

  "additionalProperties": false
}

