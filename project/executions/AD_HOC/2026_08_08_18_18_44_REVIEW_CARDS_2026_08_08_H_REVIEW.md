---
execution_id: 2026_08_08_18_18_44_REVIEW_CARDS_2026_08_08_H_REVIEW
prompt_id: PROMPT(AD_HOC:REVIEW_CARDS_2026_08_08_H_REVIEW)[2026-08-08T06:54:53+00:00]
work_item: AD_HOC
status: in_progress
rerun_of:
pr: https://github.com/xenotaur/prosoc/pull/83
commit:
agent: claude_app
instruction_source: https://github.com/xenotaur/prosoc/pull/83
session_transcript: claude-app:3c10dab8-eb6b-43aa-a003-69b92b72d689
created_at: 2026-08-08T18:18:44+00:00
---

# Summary

Address the one open review comment on PR #83 (5-card promotion session,
2026-08-08-h batch, toward `WS-NORMATIVE-PACKET-ASSEMBLY`'s full-corpus
`APPROVED` exit criterion), as part of `/lrh-land`'s review-response step.

# Result

`copilot-pull-request-reviewer` flagged that `project/design/backlog.md`'s
"APPROVED cards with unresolved should-fix findings" table is scoped by
its own heading/intro to should-fix findings, but the newly added
`constitutions/asimov_three_laws` row explicitly described a
suggestion-level discussion item, not an `audit.md` should-fix —
inconsistent placement.

Triage: presence confirmed, concern valid, feasibility trivial.
User selected option (b) (broaden the section rather than fragment into
a separate suggestions section). Fixed by:
- Renaming the section heading from "APPROVED cards with unresolved
  should-fix findings" to "APPROVED cards with unresolved findings".
- Rewording the intro paragraph to state explicitly that any finding
  severity belongs in this table, since the underlying routing gap
  (nothing revisits an already-`APPROVED` card) applies regardless of
  whether the original finding was should-fix or suggestion level.
- Grepped the repo for any other reference to the old heading text with
  a single-line pattern — initially reported none found, but this was
  wrong: the pattern missed a wrapped-across-two-lines citation in
  `prosoc/constitutions/asimov_four_laws/audit.md` (added in the PR's
  first commit, `ec7526a`), which still read "...backlog.md's 'APPROVED
  cards with unresolved should-fix findings' table." Caught by an
  independent cold-context subagent during the confirm-fixes pass (Step
  5), not by this review-response pass itself. Fixed by updating that
  line to the renamed heading. A follow-up grep using
  whitespace-normalized matching (not a raw single-line pattern)
  confirmed no further stale references remain anywhere in the repo.

Confirm gate was shown to the user (prompt ID minted, comment displayed,
two remediation options presented) before editing, per
`/lrh-review-response` Steps 3-4.

# Validation

- `git rev-parse HEAD` / `git status --short` confirmed clean state
  before commit.
- `scripts/format --check --diff` reported drift across 22 unrelated
  files — confirmed this is the known local-`black`-version false
  positive (installed `black==26.3.1` vs. CI's pinned `25.12.0`; see
  `project_prosoc_ci_black_pin.md`), not a regression from this edit.
  `backlog.md` is Markdown, not Python, so it isn't even in scope for
  `black` formatting.
- `scripts/lint` — all checks passed.
- `scripts/test` — all pass, corpus-wide `scripts/validate/status` check
  confirms all 32 cards consistent.

# Follow-up

None. `/lrh-confirm-fixes` is the next step in `/lrh-land`'s chain, to
verify this fix against the current diff and resolve the review thread.
